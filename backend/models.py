"""
Data models for the backend
Note: Flask uses dictionaries and jsonify, so models are optional
These are kept for reference/documentation purposes
"""

# Token validation response structure
# {
#     "valid": bool,
#     "payload": dict | None,
#     "error": str | None
# }
